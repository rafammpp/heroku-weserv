build:
  docker: 
    web: Dockerfile
# run:
#   web: sed -i "s/listen 80/listen $PORT/g" /etc/nginx/imagesweserv.conf && sed -i "s/listen [::]:80/listen [::]:$PORT/g" /etc/nginx/imagesweserv.conf && nginx -g 'daemon off;'